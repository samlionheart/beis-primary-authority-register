version: "2"

services:
    web:
      image: parbeta/web:v9
      container_name: par_beta_web
      links:
        - db
      ports:
        - "8111:80"
      volumes:
        - ../:/var/www/html

    db:
      image: parbeta/postgres
      container_name: par_beta_db
      restart: always
      ports:
        - "5411:5432"
      environment:
        POSTGRES_USER: par
        POSTGRES_PASSWORD: 123456
        POSTGRES_DB: par

